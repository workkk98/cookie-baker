{
  "name": "dmp-devtool",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "lerna run serve --stream",
    "build": "lerna run build --stream",
    "serve:devtool": "lerna exec --scope devtool -- yarn run serve",
    "build:devtool": "lerna exec --scope devtool -- yarn run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
